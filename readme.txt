Requirements:
pandas, numpy, matplotlib, scimitar-learn, skimage(optional, only to run HOG)

The notebook markup has specific description of what a particular section does. Run all cells till MNIST
For Running the experiment on particular dataset, run cells in that section.